---
  apiVersion: "v1"
  kind: "Service"
  metadata: 
    name: "jenkins"
  spec: 
    type: "ClusterIP"
    selector: 
      name: "jenkins"
    ports: 
      - 
        name: "http"
        port: 8080
        # nodePort: 32080
        protocol: "TCP"
      - 
        name: "slave"
        port: 50000
        # nodePort: 32081
        protocol: "TCP"
